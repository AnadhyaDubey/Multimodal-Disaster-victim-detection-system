# ==============================================
# Module 1 - RGB Occlusion Detection (Complete)
# Covers: vision/ + audio/ + fusion/ + eval/
# ==============================================

# ---- Core Deep Learning (vision/) ----
torch>=2.1.0
torchvision>=0.16.0
ultralytics>=8.3.0        # YOLO11n / YOLO26n

# ---- Image Processing (vision/) ----
opencv-python>=4.9.0

# ---- Dataset Handling (vision/) ----
pycocotools>=2.0.7        # COCO loader/filtering
roboflow>=1.1.0           # HERIDAL download (test set)

# ---- Audio Pipeline (audio/) ----
sounddevice>=0.4.6        # mic recording
librosa>=0.10.1           # spectrogram / feature extraction
soundfile>=0.12.1         # audio file I/O (librosa dependency)

# ---- Classical ML - Audio Classifier + Fusion (audio/, fusion/) ----
scikit-learn>=1.4.0

# ---- Config & Experiment Tracking (shared across vision/audio/fusion/eval) ----
hydra-core>=1.3.2
omegaconf>=2.3.0
mlflow>=2.10.0

# ---- Evaluation & Plotting (eval/) ----
matplotlib>=3.8.0

# ---- Utilities (shared) ----
numpy>=1.26.0
pandas>=2.2.0
tqdm>=4.66.0
PyYAML>=6.0.1

# ---- Linting (CI smoke test) ----
flake8>=7.0.0